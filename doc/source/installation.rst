============
Installation
============

At the command line::

    $ pip install vmware_nsx_tempest

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv vmware_nsx_tempest
    $ pip install vmware_nsx_tempest
